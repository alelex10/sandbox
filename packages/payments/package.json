{
  "name": "payments",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run --config ../../vitest.config.ts --project payments"
  },
  "dependencies": { "zod": "^3.23.0", "mercadopago": "^3.1.0" },
  "devDependencies": { "typescript": "^5.7.2" }
}
